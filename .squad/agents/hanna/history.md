# History — Hanna

## Sessions

No sessions recorded yet.

## Learnings

- M1 value is the contract-first baseline: commercial mirror, transfer validation, and high-side ingestion/publish are now mapped end to end.
- Repo docs point humans to `tests/` for validation work, but they do not yet name a single evidence home or artifact convention for M1 signoff.
- The current branch is `feature/m1-burn-down`; `main` already matches `origin/main` at `9d61616` ("Build project structure").
- 2026-05-09: M1 evidence is now explicitly anchored in `tests/README.md`, and `docs/project-structure.md` is the canonical repo map. The earlier evidence-path block is resolved.
